-- KPI: On-Spec Rate (%)
-- Math Formula: On-Spec Rate = (100 * On-Spec Batches) / Total Batches
CREATE VIEW kpi_on_spec_rate AS
SELECT
    CAST(100.0 * on_spec_batches / NULLIF(total_batches, 0) AS FLOAT) AS on_spec_percentage
FROM batch_quality_summary;